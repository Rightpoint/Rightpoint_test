export const contentful = {
    /**
     * Note: this overlaps with data exposed in environment variables.
     *
     * See .env.local
     */
    richText: {
        linkClassName: 'contentful-link',
    },
} as const
