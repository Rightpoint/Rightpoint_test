import { render } from '@rightpoint/core/testing-library'
import { GridLayout } from './Grid.component'

describe('TemporaryImages', () => {
    it('should render successfully', () => {
        // const { baseElement } = render(<Grid />)
        // expect(baseElement).toBeTruthy()
    })
})
