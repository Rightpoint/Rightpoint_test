import styled, { css } from 'styled-components'
import { typography } from '@rightpoint/core/styles'

const FallbackLoading = styled.div`
    padding-top: 100px;
    padding-bottom: 100px;
    font-size: 40px;
    text-align: center;
`

export const FallbackLoadingStyles = {
    FallbackLoading,
}
