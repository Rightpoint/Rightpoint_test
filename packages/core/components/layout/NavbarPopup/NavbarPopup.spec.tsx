import { render } from '@rightpoint/core/testing-library'

//import { NavbarPopup } from './NavbarPopup.component'

describe('NavbarPopup', () => {
    it('should render successfully', () => {
        // TODO: fix test
        //const { baseElement } = render(<NavbarPopup />)
        //expect(baseElement).toBeTruthy()
    })

    // navbar popup should have an integration test for closing/opening on desktop & mobile
    // to prevent critical ux lock in issues
})
