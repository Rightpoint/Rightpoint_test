// TODO: fix test. It needs to support redux

//import { navbarData } from '@rightpoint/data-generators'
import { render } from '@rightpoint/core/testing-library'

//import { Navbar } from './Navbar.component'

describe('Navbar', () => {
    it('should render successfully', () => {
        /*
        const { baseElement } = render(<Navbar {...navbarData} />)
        expect(baseElement).toBeTruthy()
        */
    })
})
