import styled, { css } from 'styled-components'
import { verticalSpacing } from '@rightpoint/core/styles'

export const WorkCategoryStyles = {
    Tabs: styled.div`
        ${verticalSpacing.css.small.bottom}
    `,
}
