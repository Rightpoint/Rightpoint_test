import styled, { css } from 'styled-components'
import { cssVarsTypography, media, typography } from '@rightpoint/core/styles'

export const WorkDetailPageStyles = {
    // WorkDetailPage: styled.div``,
}
